# v08 (2026-09-06) — 파이프라인 안전망 묶음 (전체 코드 리뷰 반영, 점수식 무변경)

## 무엇을 (근거: research/REVIEW_pipeline_20260906.md 의 번호)
### 워크플로 collect-data.yml
- **H1** 정본 다운로드: `gh release view` 로 자산 존재를 확인하고, 조회/다운로드 *실패* 는 중단(`exit 1`).
  "자산 없음"만 새 시작. 업로드 전 새 tar 가 직전의 50% 미만이면 중단(2차 방어).
- **H3** `concurrency: collect-us`(직렬화) · Commit 스텝 `continue-on-error: true`(푸시 실패가 DB 업로드를 막지 않음).
- **L4** 주간 백업 요일을 잡 시작 시점에 고정(`RUN_DOW`), 백업본은 **직전 일일본**(`us-data-prev.tar.gz`)으로
  — 오늘 run 이 오염돼도 백업은 무사.
- `workflow_dispatch` 입력 `repair_dates` — 부분 수집일 점수 재계산(아래 H2) 수동 트리거.

### us_page_data.py
- **H2 완전성 게이트**: 당일 시세 심볼 수 < 전일의 90% 면 CSV·score_daily·틸트 전부 생략 + ⚠️.
- `--asof D`(그날 기준 계산만, 쓰기 없음) · `--repair D1,D2`(해당 날짜 score_daily 삭제 후 재계산, 감사 로그).
  PIT: `date<=D` 행만 읽음. 게이트 미달이면 REPAIR 도 거부. CSV 는 D==최신일일 때만 씀.
- **M2** FINRA 잔고 표기 정규화 `finra_key`(BRK-B→BRKB, BAC-PB→BACPRB) — 틸트 dtc 결측 77종목 복구.
- **L5** 우선주 이름(`$`→`-P`) 매핑.

### us_notify_test.py
- **M10** 부분 수집일엔 순위 대신 ⚠️ 경고 메시지(휴장 가드와 무관하게 전송). 평일인데 오늘 시세가 없으면
  "시세 없음 — 휴장일이면 정상" 한 줄 전송(연 ~10회 휴장일 잡음 감수). 건강줄에 "최신일 n행(전일 대비 %)".

### us_ohlcv_collector.py
- **§7 큐 굶음**: 이벤트에 날짜를 붙여 `event_checked`(symbol,date,kind) 로 1회만 처리(한 배당당 ~5회
  재수집이 원인) · 처리 순서 split→cliff→div · cliff 대기 심볼에 진짜 split 이 오면 승격(`register_event`).
- **M4** 배치 fetch 예외도 attempts+1(무한 재시도 방지), 포기 3→6회.
- **H4** 증분 창 = MAX(date)−7d(마지막 저장일이 7일 넘게 오래됐을 때만 넓어짐) · 평일 증분 0행이면 ⚠️.
- **M3** `sub_frame()` — 컬럼이 MultiIndex 인지로 판단(yfinance 1.x 는 1심볼도 MultiIndex).
- **L1** close/adj_close ≤ 0 행 저장 안 함.

### us_options_collector.py
- **M1** 빈 만기 목록(yfinance 가 오류를 `()` 로 주는 경우) → 1회 재시도 후 `empty` 카운터,
  snapshot_log 에 `empty` 컬럼(구 DB 는 ALTER 로 추가, 재실행 무해), 완주율 <80% 면 ⚠️.

### us_seed_collector.py
- **M9** 지수 구성종목 표를 못 찾으면 ⚠️(NDX100 40일 0행 무음). **L3** 상장목록 파일당 <1000행이면
  그날 스냅샷·이벤트 생략(가짜 상폐 이벤트 영구 기록 방지) — 멤버십 수집은 계속.

## 바뀐 파일
.github/workflows/collect-data.yml · us_page_data.py · us_notify_test.py · us_ohlcv_collector.py ·
us_options_collector.py · us_seed_collector.py · patch_note/v08_20260906.md · US_PROJECT_KNOWLEDGE.md(§5·§7)

## 검증 (오프라인, 09-05 Release DB 사본)
- **0-diff**: us_page_data 정상 실행 결과 3,361종목이 저장된 score_daily 20260904 와 순위 전부 동일
  (score 차이 ≤ 0.0005 = CSV 반올림). 점수식·가드 무변경.
- `--repair 20260902,20260903`: 1,816/2,378행 삭제 → 3,368/3,363행 재적재, 틸트 50행 재적재, CSV 미변경 확인.
  (실측 top10 예: 20260902 저장본 `CAKE,BMY,EXPE…` → 재계산 `LITE,MPC,MRNA…`)
- 부분 수집 시뮬레이션(20260904 심볼 J~ 삭제 → 47%): 정상 실행·REPAIR 모두 생략 확인, notify 는 ⚠️ 메시지.
- ohlcv self-test 22항목 통과 + 가짜 fetch 로 process_queue 시뮬레이션(배치 예외 → attempts 1, 재실행 시
  split→cliff→div 순 처리, event_checked/cliff_checked 기록, adj 소급 반영).
- options self-test 13항목(마이그레이션 포함) · seed 상장목록 가드 오프라인 확인 · YAML 파싱 · py_compile.
- **미검증(환경상 불가)**: 러너 실행. 첫 실행 로그에서 볼 것 — "[재조정] 큐 …" 가 cliff 를 처리하는지,
  MNST 20260811 adj_close 가 고쳐지는지(며칠 내), 옵션 "빈체인 n", 텔레그램 건강줄 "최신일 … (97%)".

## 남은 한계 / 후속
- 이미 박제된 20260902·20260903 점수는 **사용자가 Run workflow → repair_dates=`20260902,20260903`** 로 1회 복구.
  (관측 우선 원칙의 예외 — 부분 수집 결함 복구이며 감사 로그가 남는다. 지식문서 §5 기록.)
- 7월 초기 미조정 역분할 오염(mom12<−0.99, 110행)은 그대로 둠 — OOS 판정 시 제외 규칙으로 처리(리뷰 L9).
- M5(xbrl start 컬럼·재적재)·M7(accepted_et)·M8(insider cik 조인)·L2(UTC 라벨)·L6~L8 은 다음 라운드.
- 큐 backlog(cliff 775)는 회당 200 이라 3~4일에 걸쳐 소화. 그동안 페이지 점수는 일부 절벽 종목 오염 지속.

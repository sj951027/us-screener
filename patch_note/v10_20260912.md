# v10 (2026-09-12) — 수집 실패 계측(러너 지문·사유 집계) · v09 2차 수집 제거 · 의존성 고정 (점수식 무변경)

## 무엇을 (근거: 러너 #55 원본 로그 · 정본 tar 09-12 00:52 UTC · 로컬 대조 실험)

### 진단부터 (이번 패치의 근거)

러너 #55(2026-09-12 00:00 UTC, v09 코드)는 success 로 끝났지만 09-11 시세를 **1,055/6,551심볼(16%)** 만 받았다.
v09 게이트가 적재를 막아 오염은 없었고(score_daily 09-11 미적재), 텔레그램 경고도 나갔다. 문제는 원인을 못 좁힌 것이다.

| 확인한 것 | 결과 | 함의 |
|---|---|---|
| v09 2차 수집(90s 뒤 결측 5,504심볼 재요청) | **+0행**, 배치 예외 0 | 같은 실행=같은 출구 IP 라 즉시 재요청은 무의미 |
| 로컬(노트북)에서 결측 20종목 조회 | 20/20 에 20260911 행 | 데이터는 야후에 있다 |
| 로컬에 러너와 동일 버전(yfinance 1.7.0 + pandas 3.0.5) 구성 후 같은 코드 경로 | 30종목 중 29개 정상(1개는 실제 상폐) | 라이브러리 버전 문제 아님 |
| 결측의 알파벳 10분위 분포 | 전 구간 11~21% 로 균일 | 배치 순서·누적 요청량 탓이 아님(09-10 의 S~Z 편중과 다른 양상) |
| 하이픈 심볼 vs 일반 심볼 수집률 | **93% vs 11%** | 구조적 차이 존재, 메커니즘 불명 |
| 러너 Azure 지역 | #53·#54 westcentralus(정상) / #55 eastus2(16%) | 러너별 차이 가능성 |

**원인 미특정.** 못 좁힌 이유는 수집기가 심볼별 실패를 `except Exception: pass` 로 삼켜, 러너에서 어떤 응답이
왔는지 기록이 전혀 남지 않기 때문이다. 그래서 이번 패치는 고치는 게 아니라 **보이게 만드는 것**이다.

### us_ohlcv_collector.py
- **`runtime_banner()`** — 로그 첫 줄에 `[환경] 출구IP … · yfinance … · pandas … · curl_cffi …`.
  IP 는 워크플로가 `RUNNER_IP` 로 넣어준다. 러너별 수집 수확량을 사후에 대조할 수 있게 된다.
- **`YFLogCapture` + `_norm_msg()`** — yfinance 로거의 WARNING 이상을 모아 **사유별로 집계**해서 찍는다
  (`[증분 사유] 경고/오류 N건 · 종류 M` + 상위 8종, 심볼 예시 1개). 심볼명은 `$SYM:` 으로 정규화해 묶는다.
  로컬 실측에서 `$SYM: No data found, symbol may be delisted` 와 `HTTP Error 404: {...}` 본문까지 잡히는 것을 확인했다.
- **심볼별 결측 분류** — 증분 루프의 `except Exception: pass` 를 `빈 프레임 / 응답에 심볼 없음(KeyError) / 그 외 예외`
  3분류 카운터로 대체. `[증분 결측] 빈 프레임 N · 응답에 심볼 없음 N · 예외 N (대상 N)`.
- **수확량 점검** — 최신일 수집률이 `LOW_YIELD_FRAC`(0.9) 미만이면 결측의 **하이픈/일반 비율**과 예시 심볼을 찍는다.
  재요청은 하지 않는다.
- **v09 2차 수집 제거** — `retry_missing()` 과 호출부, 상수 `RETRY_MIN_FRAC/RETRY_WAIT_S/RETRY_CHUNK/RETRY_SLEEP` 삭제.
  효과 0행이 실측으로 나왔고 배치 276개·약 14분을 더 쓴다. `missing_latest()` 는 계측용으로 남긴다.

### .github/workflows/collect-data.yml
- **`Runner fingerprint` 스텝 신설**(수집기 앞) — `api.ipify.org`, 실패 시 `checkip.amazonaws.com` 으로 공인 IP 를
  받아 `GITHUB_ENV` 에 `RUNNER_IP` 로 넣고 출력. `continue-on-error: true` 로 비치명.
  이 IP 는 GitHub 호스티드 러너(Azure)의 일회용 주소다. 공개 저장소 로그에 남아도 개인 정보가 아니다.

### requirements.txt
- **버전 고정**(`requests==2.34.2`, `pandas==3.0.5`, `lxml==6.1.3`, `yfinance==1.7.0`). 지금까지는 매 실행 최신본을
  받아 왔다. 야후는 비공식 소스라 라이브러리가 조용히 바뀌면 수집 동작도 조용히 바뀐다. 이번엔 버전이 원인이
  아니었지만, 원인을 좁히는 동안 변수를 하나 줄인다. 실제 설치본은 `[환경]` 줄로 계속 확인한다.

## 왜 이 순서인가

떠올릴 수 있는 대응(옵션 수집 축소, 절벽 큐 축소, 같은 날 재시도 cron 추가)은 전부 **원인 가설에 딸린 추측**이다.
옵션 스냅샷은 소급이 안 돼서 줄이면 데이터가 영구히 사라지고, cron 추가는 요청량이 원인일 경우 역효과다.
비용이 확실하고 효과가 불확실한 선택을 먼저 하지 않는다. 계측은 어느 가설에서도 옳고, 실행 1회면 답이 나온다.

한편 하루를 놓쳐도 영구 손실은 **옵션 스냅샷뿐**이다. score_daily 는 v09 자동 보충이 시점 정보를 지키며 채우고
(실측 러너 #54: 20260909 자동 보충 3,356행), 페이지는 다음 실행이 따라잡는다.

## 바뀐 파일

`us_ohlcv_collector.py` · `.github/workflows/collect-data.yml` · `requirements.txt` ·
`patch_note/v10_20260912.md` · `US_PROJECT_KNOWLEDGE.md`(§5·§7)

## 검증 방법 (오프라인)

- `python us_ohlcv_collector.py --self-test` **38항목 통과**(신규 10: 메시지 정규화 4 · 로그 캡처 6).
  캡처 테스트는 WARNING+ 만 잡는지, 같은 사유가 묶이는지, 다른 사유가 안 묶이는지, 예시 심볼이 남는지,
  블록을 빠져나온 뒤 핸들러가 제거되는지, 경고 0건일 때 그 사실을 찍는지를 본다. v09 의 retry 검증 4항목은 삭제.
- 가짜 로그 1,230건(2종)으로 `report()` 출력 확인 — 상위 2종이 횟수·예시와 함께 2줄로 요약됨.
- `runtime_banner()` 단독 호출로 `[환경] 출구IP 203.0.113.7 · yfinance 1.4.0 · pandas 2.3.3 · curl_cffi 0.15.0` 확인.
- 워크플로 YAML 파싱: 스텝 13개, `Runner fingerprint` 가 `Run collectors` 바로 앞에 위치.
- 점수 경로(us_page_data.py·us_notify_test.py)는 **건드리지 않았다** → 점수식 0-diff.

## 첫 러너 검증 (러너 #56, 2026-09-12 02:26 UTC 수동 실행)

계측이 의도대로 찍혔고, 같은 실행이 09-11 결손도 복구했다.

```
Azure Region: westus3
출구 IP: 20.171.126.211
[환경] 출구IP 20.171.126.211 · yfinance 1.7.0 · pandas 3.0.5 · curl_cffi 0.16.3
증분 완료: 신규 5496행 · 이벤트 신규등록 78심볼 · 배치 실패 0
[증분 결측] 빈 프레임 3 · 응답에 심볼 없음 0 · 예외 0 (대상 6562)
[증분 사유] 경고/오류 11건 · 종류 7
         3회 | N Failed downloads:
         2회 | $SYM: possibly delisted; no price data found (period=7d)  (예: FBYDP)
         1회 | HTTP Error 401: {"finance":...,"description":"Invalid Crumb"}
         1회 | HTTP Error 401: {..."User is unable to access this feature..."}
         1회 | $SYM: No data found, symbol may be delisted  (예: SVA)
최신일 20260911 수집 6,543/6,551심볼 — 정상
저장: docs/data/us_latest.csv · 3,352종목 · 기준일 20260911
score_daily 적재: 신규 3352행 · 누적 148,119행/44일 (model=us_mus_v0)
```

**정상 실행의 기준선**: 결측 3 / 경고 11건. 다음에 수확량이 낮은 실행이 나오면 이 숫자와 대조하면 된다.
`[증분 사유]` 가 401/429 로 뒤덮이면 접근 거부, `possibly delisted` 로 뒤덮이면 야후가 빈 응답을 준 것,
`응답에 심볼 없음` 이 크면 배치 응답 자체가 잘린 것이다.

### 러너 지문 누적 (원인 후보 대조용)

| 러너 | Azure 지역 | 출구 IP | 최신일 수집률 |
|---|---|---|---|
| #53 (09-11 수동) | westcentralus | (v10 이전) | 정상 |
| #54 (09-11 수동) | westcentralus | (v10 이전) | 정상 |
| #55 (09-12 스케줄) | eastus2 | (v10 이전) | **16%** |
| #56 (09-12 수동) | westus3 | 20.171.126.211 | 99.9% |

4건 중 유일한 실패가 eastus2 다. 표본이 작아 결론이 아니라 **관찰**이다. 지역·IP 별 수집률이 더 쌓여야 한다.

## 남은 한계

- **원인은 아직 모른다.** 이 패치는 다음 실행 로그에서 원인을 좁히기 위한 것이다. 볼 줄:
  `[환경] 출구IP` · `[증분 결측]` 3분류 · `[증분 사유]` 상위 메시지 · 수확량 점검의 하이픈/일반 비율.
- 하이픈 93% vs 일반 11% 의 메커니즘은 로컬에서 재현되지 않는다(로컬은 둘 다 정상). 러너에서만 나타나는 현상이라
  러너 로그 없이는 더 좁힐 수 없다.
- 러너 IP 가 실행마다 다른지는 이 패치가 적용된 실행이 2회 이상 쌓여야 확인된다. Azure 지역은 로그 헤더
  (`Azure Region:`)에 원래 찍히므로 IP 와 함께 대조하면 된다.
- ~~09-11 시세는 아직 16% 상태다.~~ → 러너 #56 이 6,543/6,551(99.9%)로 채웠고 score_daily 20260911 3,352행 ·
  페이지 기준일 20260911 로 갱신(커밋 62b9aca). 09-01~09-11 연속 44일 기록 확보.
- 절벽 큐(잔여 1,136)는 이번에도 손대지 않았다. 회당 200심볼 전체 이력 재수집이지만 요청 **건수**로는 200건이라
  6,562건짜리 증분에 비하면 작다 — 요청량 다이어트의 우선순위가 아니라는 뜻. 별건으로 남긴다.

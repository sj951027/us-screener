# v10 2026-09-12: 버전 고정. 러너는 매 실행 최신본을 받아 왔고(실측 #55 yfinance 1.7.0 + pandas 3.0.5),
#   야후는 비공식 소스라 라이브러리가 조용히 바뀌면 수집 동작도 조용히 바뀐다. 실제 설치본은
#   us_ohlcv_collector 의 [환경] 줄에 찍힌다. 올릴 때는 로컬에서 self-test + 0-diff 확인 후 이 파일만 수정.
requests==2.34.2
pandas==3.0.5
lxml==6.1.3
yfinance==1.7.0

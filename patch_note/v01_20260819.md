# v01 (2026-08-19) — SEC 403 대응 + us.html 필터 2종

## 무엇을
1. `.github/workflows/collect-data.yml` — `Run collectors` env 에
   `SEC_USER_AGENT: "us-screener seok5139@gmail.com"` 추가(코드 0줄 수정).
2. `docs/us.html` — 필터 버튼 2종 추가: `1주 상승`(w1pos), `1개월 하락`(m1neg).
   조합 시 "중기 조정 중 단기 반등" 관측 렌즈. 설명문단에 근거·경고 병기.

## 왜
- SEC 3종 수집기(XBRL·실적일·Form 4)가 Actions 35회 전부 403 Forbidden → 0행 적재.
  기본 UA(괄호·세미콜론·URL 포함)가 차단 용의자. 실측 근거: US_PROJECT_KNOWLEDGE.md §7.
- 필터: 사용자 요청. 52주고점 음수 조건은 정의상 무의미(97%가 음수)라 제외.

## 검증
- 필터: 실페이지 JS 를 추출해 8/14 CSV 3,362행으로 오프라인 실행 —
  m1pos+m1neg+(0) = 전체 행수 일치, 기존 필터 7종 무영향, HTML 태그 균형 OK.
- UA: 적용 후 첫 주간 사이클에서 검증(→ v02 시점, 8/21 실측 3종 전부 적재 성공 확인).

## 남은 한계
- 없음(v02 에서 해결 확인됨).

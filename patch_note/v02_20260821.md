# v02 (2026-08-21~22) — 수집기 분할 소급 재조정 (us_ohlcv_collector)

## 무엇을
`us_ohlcv_collector.py` v2026-08-21: 3중 방어 —
1. 일일 증분 fetch `actions=True` → 창 안 분할/배당 이벤트 감지 시 `adjust_queue` 등록
2. 매 실행 절벽 스캔(인접일 adj_close 정수분할비 ±6%) — 오탐(실제 급등락)이어도
   재수집은 같은 값 덮어쓰기라 무해. `cliff_checked` 로 절벽당 1회만.
3. 큐 처리 — 심볼 전체 이력 재수집 `INSERT OR REPLACE`, 회당 200 상한,
   0행 3회 실패 시 포기(상폐 심볼의 큐 영구 점유 방지).
신규 테이블: `adjust_queue`, `cliff_checked`. 백필·재시도 경로 무변경.

### 8/22 결함 수정 2건 (배포 전 자체 검토에서 발견)
- 재수집 시작일: 오늘−3년 → **심볼의 최고(最古) 저장일 −7일** (원백필보다 늦게 시작해
  경계에 새 가짜 절벽이 남는 문제).
- 다중 절벽 심볼(예: AEHL 5절벽): 절벽당 1회씩 재수집 → 심볼당 날짜 집계 1회 처리.
- 큐 우선순위 split>div>cliff 명시.

## 왜
증분 7일 창은 그 이전 행을 안 건드림 → 백필 이후 분할이 소급 반영되지 않아
adj_close 가짜 절벽(실측: MNST 2:1 20260811, $91.43→$45.53, 조정 부재).
점수(mom12)·성과추적·OOS 판정 데이터 전부 오염 위험. US_PROJECT_KNOWLEDGE.md §7.

## 검증 (전부 오프라인)
- `--self-test` 13항목 통과(감지 3·스캔 3·저장 3·큐 4). yfinance 불필요.
- 실DB(8/20 tar) 스캔 11초, 절벽 1,251건/761심볼(MNST 포함) — 초회 큐는 200/일 상한으로
  약 4일에 걸쳐 소화. v2 시뮬레이션: 심볼 단위 집계 762행, split 최우선 정렬,
  MNST 재수집 시작 2023-07-06(전체 이력 커버) 확인.
- 0-diff: 이벤트 없는 심볼의 증분·백필 동작은 이전과 동일(store 기본 IGNORE 유지).

## 남은 한계
- 증분 창을 벗어난 과거 배당의 소급 드리프트(연 1~2%)는 미감지 — 연 1회 전체
  재백필로 보정하는 방안 미결정(§7).

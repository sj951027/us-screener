# v03 (2026-08-22) — 텔레그램 DB 건강 요약 (us_notify_test)

## 무엇을
`us_notify_test.py` 에 `db_health()` 추가 — 매일 알림 하단에 한 줄:
`🗄 시세 470만 · 공매도 20260731 · 재무 167만 · 실적일 42만 · 내부자 24만`
빈 테이블은 `⚠️0` + 별도 경고줄. 분할 재조정 잔여 큐도 있으면 표시. 조회 실패는 '?'(비치명).

## 왜
SEC 3종이 403으로 **35회 연속 0행**인데 워크플로는 전부 녹색이었던 사고(§5 2026-08-19).
"수집기 완료 ≠ 데이터 존재" — 핵심 테이블 행수를 사람 눈이 매일 보게 한다.

## 검증
- 8/20 실DB dry-run: 요약줄 정상 출력(위 예시가 실측값).
- 빈 DB 픽스처: 5개 테이블 전부 `⚠️0` + 경고줄 출력 확인.
- 기존 메시지·휴장일 가드·전송 로직 무변경(요약 삽입은 try/except 비치명).

## 남은 한계
- 행수 '전일 대비 증분'은 미표시(상태 저장 필요) — 필요성 확인되면 추가.

# v04 (2026-08-30) — 내부자 수집기 관계플래그 버그 수정 (us_insider_collector)

## 무엇을
`us_insider_collector.py` v04 (파서 v2):
1. REPORTINGOWNER 파싱에 `RPTOWNER_RELATIONSHIP` 텍스트 폴백 추가 — 불리언 컬럼이
   없으면 관계 텍스트(OFFICER/DIRECTOR/TENPERCENTOWNER, 콤마 결합 포함)에서
   is_officer/is_director/is_tenpct 를 도출. 불리언·텍스트 둘 다 없으면 경고 출력
   (행은 플래그 0으로 보존).
2. `PARSER_VER` 게이트: DB 에 기록된 파서 버전과 다르면 insider_files_done 을 1회
   초기화 → 기존 백필 전 분기를 자동 재적재(회당 4분기, delete-then-insert 라
   idempotent). 코드 커밋만으로 재백필이 시작되고 워크플로 수정은 불필요.
3. self-test 에 실데이터 형식 픽스처(_fixture_zip_rel: 불리언 컬럼 없음,
   RELATIONSHIP 텍스트만) 3항목 추가.

## 왜 (실측 근거)
- insider_tx 858,373행 전체에서 관계 플래그가 0 (2026-08-30 Release tar 실측,
  research/RESEARCH_us_sec_scan_20260830.md 에서 발견). 임원/이사 필터가 불가능해
  내부자 신호 스캔이 막혔다.
- 원인: SEC 공식 insider_transactions_readme §5.2 — 실데이터 REPORTINGOWNER 에는
  IS_OFFICER 류 불리언 컬럼이 없고 단일 `RPTOWNER_RELATIONSHIP` 텍스트 필드만 있다.
  수집기 `_col` 후보("rptowner_isofficer"/"isofficer"/"officer")가 전부 미탐지 →
  `flag()` 가 조용히 0 반환. 기존 self-test 픽스처는 불리언 형식이라 통과해 결함이
  가려졌다 — 픽스처는 실데이터 형식을 복제해야 한다는 교훈.

## 바뀐 파일
- us_insider_collector.py (+58줄 diff, 순수 추가 위주 — GitHub main 대비 diff 확인)
- US_PROJECT_KNOWLEDGE.md §3 표·§7 트러블슈팅 항목 추가
- patch_note/v04_20260830.md (이 문서)

## 검증 (전부 오프라인·실측)
- self-test 10항목 전부 통과(기존 7 + 신규 3: 실데이터형 파싱, 'Officer'→플래그,
  'Director,TenPercentOwner'→복수 플래그).
- PARSER_VER 게이트 시뮬레이션: 가짜 done 2분기 → 1회차 초기화·meta 기록,
  2회차(버전 일치) 유지 확인. 네트워크 차단 상태에서 게이트까지만 실행해 검증.
- GitHub main 원본과 diff 로 삽입 범위 확인(파싱·게이트·픽스처 외 변경 없음).
- 미검증(환경상 불가): 실제 분기 zip 파싱 — 컨테이너·로컬 모두 sec.gov 차단.
  다음 Actions 실행 로그의 "♻ 재백필 예약" 줄과, 재백필 분기의
  `SELECT is_officer, COUNT(*) FROM insider_tx GROUP BY 1` 분포로 확인할 것.

## 남은 한계
- 한 accession 에 신고인이 여럿인 경우 첫 신고인만 기록(기존 동작 유지).
- 재백필 완료까지 ~8일(31분기/회당4) — 그동안 insider_tx 는 분기별로 신구 혼재.
  스캔은 재백필 완료 후 권장.
- RPTOWNER_RELATIONSHIP 값이 예상 밖 표기일 가능성은 실파일 미확인 상태 — 위
  분포 쿼리에서 전부 0이 재현되면 실파일 헤더 덤프부터 다시 볼 것.

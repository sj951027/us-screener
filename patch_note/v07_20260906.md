# v07 (2026-09-06) — FINRA 일별 공매도 거래량 수집기 신설 (us_shortvol_collector, 데이터 확장 5차)

## 무엇을
1. `us_shortvol_collector.py` 신설 → `us_shortvol.db` `short_volume_daily(date, symbol, short_vol,
   short_exempt_vol, total_vol, market)` PK(date,symbol) + `shortvol_files_done`.
   소스 `cdn.finra.org/equity/regsho/daily/CNMSshvol{YYYYMMDD}.txt`(통합 NMS). daily_ohlcv 심볼로
   제한(용량). 일일: 최근 10일 탐침. 백필: `--backfill-from 2023-05-01` 회당 150파일(≈6회).
   idempotent·비치명·포맷 감지 실패 시 raw_finra/ 보존·심볼 `.`→`-` 통일.
2. 워크플로 `Run collectors`: `python us_shortvol_collector.py --backfill-from 2023-05-01` 1줄
   (완료 후에도 done 건너뜀이라 무해). tar·무결성 게이트는 *.db 글롭이라 자동 포함.
3. `us_notify_test.py` db_health: `us_shortvol.db` 존재 시 "일별공매도 <최신일>" 항목 추가
   (조용한 실패 감시 — 8/19 교훈).

## 왜 (사용자 결정 2026-09-06)
"근본적으로 새 모델"은 새 재료에서 — 무료·과거 파일 공개·바로 3년치 스캔 가능한 소스 중 1순위.
기존 short_interest 는 격주 '잔고', 이것은 매일 '체결 중 공매도 비율'(문헌: 비율↑ → 이후 부진,
사전 부호 −). 순발행 스캔(같은 날, research/RESEARCH_us_nsi_scan_20260906.md)은 무신호로 종결.
설계 근거: research/DESIGN_us_shortvol_collector_20260906.md.

## 바뀐 파일
- us_shortvol_collector.py (신규) · .github/workflows/collect-data.yml (+1줄+주석) ·
  us_notify_test.py (+3줄) · US_PROJECT_KNOWLEDGE.md §3 표 행 추가 · patch_note/v07_20260906.md
- research/draft_us_shortvol_collector.py 는 초안(루트 파일과 헤더 1줄만 다름) — 삭제해도 됨.

## 검증
- **실파일 확인(2026-09-06, 사용자 Chrome 으로 cdn.finra.org 직접 열람)**: CNMSshvol20260904.txt
  (12,215행)와 백필 시작일 CNMSshvol20230501.txt(9,741행) 둘 다 존재, 형식 동일 —
  헤더 `Date|Symbol|ShortVolume|ShortExemptVolume|TotalVolume|Market`, 파이프, 거래량은 소수
  (예: `20260904|AAPL|7632509.075240|27953|14537792.715407|B,Q,N`), 마지막 줄 레코드 수 트레일러.
- **표기 불일치 발견·수정**: FINRA 는 클래스 `BRK/B`, 우선주 `ABRpD`, 워런트 `AAC/WS` 표기 —
  초안은 `.`만 처리했음. `norm_symbol()` 추가(`/`→`-`, `p+대문자`→`-P`+문자) → daily_ohlcv 표기
  (BRK-B, ABR-PD, AKO-A — DB 실측)와 일치.
- 커버리지 실측: 우리 유니버스($5 이상 4,816심볼) 무작위 300개 중 296개(98.7%)가 09-04 파일에
  존재. 누락 4개(GJR, FGNXP, MZYX, WHLRP)는 특수 종류.
- 오프라인 self-test 8항목 통과(픽스처를 실파일 형식으로 복제 — v04 교훈)·py_compile·YAML 파싱.
- 러너 실행은 미검증: 첫 러너 로그 "💾 short_volume_daily 신규 n파일 · 누적 n행" 과 텔레그램
  "일별공매도 <날짜>" 로 확인.

## 남은 한계
- 게시 시각(18:00 ET 이전)이 러너(22:00 UTC=18:00 ET)와 겹쳐 당일 파일은 다음 실행에서 —
  정상 동작. 스캔 PIT 규칙: 거래일 D 파일은 D+1 이후 앵커에서만.
- 용량 ~250MB(3.3년, 추정)·연 ~80MB 증가 → §6-1 2GB 캘린더 약간 앞당김. 2018년 소급은 보류.
- 러너 시간 백필 중 +1~2분(150파일×0.3s+다운로드), 이후 +수초.
- FINRA 비상업 이용 조건 — 개인 조회 전용 프로젝트라 부합(법률 판단 아님).
